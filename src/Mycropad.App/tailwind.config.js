module.exports = {
    content: [
        './**/*.html',
        './**/*.razor'
    ],
    darkMode: 'media', // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {
            mixBlendMode: ['hover'],
        },
    },
    plugins: [],
}
